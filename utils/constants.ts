export enum role {
  USER_ROLE = "user",
  PROCESSING_ROLE = "bot-processing",
  ERROR_ROLE = "bot-error",
  BOT_ROLE = "bot",
  BOT_STREAM_ROLE = "bot-stream",
}

export enum responseType {
  AUTH = "auth",
  PROCESSING = "processing",
  RESPONSE = "response",
  QUERY = "query",
  ERROR = "error",
}
